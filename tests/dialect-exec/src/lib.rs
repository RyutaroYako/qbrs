//! Nothing lives here. The crate exists for its `tests/`, where every SQL
//! shape qbrs renders for the `Sqlite` dialect is handed to a real SQLite
//! and executed. That is a claim no string assertion can make.
