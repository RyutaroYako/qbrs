[package]
name = "compile-bench"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
qbrs-core = { path = "../../crates/core" }

# The `trybuild-drafts` are copied into `examples/` to eyeball diagnostics,
# and some of them use the derive, which lives behind the facade.
[dev-dependencies]
qbrs = { path = "../../crates/qbrs" }
