[package]
name = "qbrs-macros"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true
categories = ["database", "development-tools::procedural-macro-helpers"]
keywords = ["sql", "database", "query-builder", "derive", "macros"]
description = "Proc macros for qbrs (schema derive, sql! escape hatch)."

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0.107"
quote = "1.0.47"
syn = { version = "3.0.3", features = ["full", "extra-traits"] }
